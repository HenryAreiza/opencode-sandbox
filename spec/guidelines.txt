1. Addapt the repo to use custom docker images too, because some projects need specialized environments and we have to addapt to them instead of trying to addap those projects to this opencode-sandbox.
 - Consider that several containers should be abble to coexist at the same time, so you should avoid name conflicts on this regard.
 - This function could use something like a subfolder called "opencode-sandbox" inside the project main folder, so the user can put in there the Dockerfile of the custom image, the packages and requirements files, etc; and even better, the AI agents would be abble to update those files while the image changes to stay always up to date in case the user needs to restart the container at certain point. The user should be abble to include this folder in the gitignore of the project if required.
 - Create a new md file that the user should include in the specs of the project. This file has the purpose of instruct the AI agents about how to keep the custom image files (those inside "opencode-sandbox" subfolder) up to date after every change to the image. The idea is that the user can copy and paste this file inside his project and just reference it to the agents so they'll know how to work with opencode-sandbox.
2. Add an option to keep the container created even after closing the session (no ephemeral containers option activated by default but configurable). This option should work for both the default container and the optional custom container.
3. Update the README file with detailed instructions about how to use the custom container and the new options, without removing any of the current functionalities.
4. Create a new folder called "test", where you'll put the instructions and all necessary files to tun a simple example about how to use the new custom container functionality. You don't have to run this test, just leave everyting prepared and I'll test everyting by myself when you finish and then I'll give you my feedback to correct or change anything if necessary.
